import { Selfie } from './selfie';

/**
 * ClasseClasse représentant le wookie d'un selfie, et le wookie qui peut se connecter
 */
export class Wookie {
    nom: string;
    selfies: Selfie[] = [];
}
